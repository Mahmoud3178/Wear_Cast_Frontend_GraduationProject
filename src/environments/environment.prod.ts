export const environment = {
  production: true,
  apiUrl: 'https://wear-cast.runasp.net',
  /** Virtual try-on API (ngrok / dedicated host). Must allow CORS from your deployed origin. */
  tryOnApiBase: 'https://typically-wheylike-magen.ngrok-free.dev'
};
