export const environment = {
  production: true,
  apiUrl: 'https://wear-cast.runasp.net',
  /** Same-origin prefix; Vercel rewrites to try-on host (avoids browser CORS). */
  tryOnApiBase: '/tryon-service'
};
